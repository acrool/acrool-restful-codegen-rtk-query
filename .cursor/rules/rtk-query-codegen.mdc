---
description: RTK Query OpenAPI 代碼生成工具，用於從 OpenAPI 文檔自動生成 RTK Query hooks
globs: 
alwaysApply: false
---
# RTK Query OpenAPI 代碼生成規則

## Headers 處理規則

### 全局處理
1. API 請求的 headers（如 `Authorization`、`Client-Id` 等）應該在 baseApi 中統一處理
2. 這些 headers 不應該在每個生成的 endpoint 中重複定義
3. 代碼生成器會自動忽略 OpenAPI 規範中的 header 參數

### 實現方式
1. 在 `generate.ts` 中過濾掉所有 header 類型的參數：
   ```typescript
   const parameters = supportDeepObjects([...pathItemParameters, ...operationParameters])
     .filter(argumentMatches(overrides?.parameterFilter))
     .filter(param => param.in !== 'header');
   ```

2. 在生成的 query 函數中移除 headers 屬性：
   ```typescript
   createObjectLiteralExpression([
     // url, method, body, cookies, params 等屬性
     // 不包含 headers
   ])
   ```

### 使用方式
1. 在 baseApi 中設置全局 headers：
   ```typescript
   import { createApi } from '@reduxjs/toolkit/query/react'

   export const baseApi = createApi({
     baseQuery: fetchBaseQuery({
       prepareHeaders: (headers, { getState }) => {
         // 在這裡添加全局 headers
         headers.set('Client-Id', 'YOUR_CLIENT_ID')
         const token = (getState() as RootState).auth.token
         if (token) {
           headers.set('Authorization', `Bearer ${token}`)
         }
         return headers
       },
     }),
     endpoints: () => ({}),
   })
   ```

## 參數處理規則

### Query 參數
- 保留在生成的代碼中
- 可以通過 `encodeQueryParams` 選項控制是否進行 URL 編碼

### Path 參數
- 保留在生成的代碼中
- 可以通過 `encodePathParams` 選項控制是否進行 URL 編碼

### Cookie 參數
- 保留在生成的代碼中
- 通過 `cookies` 屬性傳遞

### Body 參數
- 保留在生成的代碼中
- 直接作為 `body` 屬性傳遞

## 注意事項

1. 代碼生成後，所有的 header 參數都會被移除，不會出現在生成的類型定義中
2. 如果需要自定義某些 endpoint 的 headers，應該通過 RTK Query 的 `extraOptions` 或者在 baseQuery 中處理
3. 這種設計可以確保：
    - 更好的代碼復用性
    - 更容易的認證管理
    - 更清晰的類型定義
    - 減少重複代碼

## 專案使用說明

### 基本介紹
這是一個用於生成 RTK Query hooks 的開源工具，可以自動從 OpenAPI 文檔生成對應的 RTK Query hooks。

### 安裝和構建
```bash
# 安裝依賴
yarn install

# 構建專案
yarn build
```

### 使用方法
```bash
# 生成 RTK Query hooks
npx acrool-rtk-query-codegen-openapi ./rtk-query-codegen-slice.config.ts
```

### 配置文件說明
配置文件（rtk-query-codegen-slice.config.ts）主要包含以下選項：

```typescript
const config: ConfigFile = {
  schemaFile: 'openapi.json',        // OpenAPI 文檔路徑
  apiFile: './baseApi.ts',          // 基礎 API 文件路徑
  apiImport: 'baseApi',             // API 導入名稱
  outputFiles: generateOutputFiles(['auth', 'audit', 'operatorId']), // 輸出文件配置
  hooks: true,                      // 是否生成 hooks
  useEnumType: true,                // 是否使用枚舉類型
  sharedTypesFile: './test_output/shared-types.ts', // 共用類型文件
};
```

### 輸出文件配置
可以通過 `generateOutputFiles` 函數配置多個輸出文件：

```typescript
const generateOutputFiles = (keys: string[]) => {
  return Object.fromEntries(
    keys.map(key => {
      const operationNameSuffix = capitalize(key);
      const targetPath = getTargetPath(key);
      return [
        targetPath,
        {
          filterEndpoints: [new RegExp(`^(get|post|put|delete)${operationNameSuffix}`, 'i')],
          typeOutputDir: './types',
          exportTypes: true,
        }
      ];
    })
  );
};
```

### 開發相關
- 使用 TypeScript 開發
- 使用 tsup 進行構建
- 使用 vitest 進行測試
- 支持 watch 模式開發

### 主要特點
1. 支持按模塊分割生成文件
2. 支持自定義過濾端點
3. 支持類型導出
4. 支持共用類型定義
5. 自動處理 headers
6. 支持多種參數類型（query、path、cookie、body） 

## 代碼結構和共用類型處理

### 代碼生成流程
1. 首次生成：為了提取共用類型
   - 先執行一次完整的代碼生成
   - 收集所有生成的類型定義
   - 分析並識別共用類型
   - 將共用類型提取到 `shared-types.ts` 文件中

2. 實際生成：
   - 使用提取出的共用類型
   - 生成最終的 API 代碼
   - 確保所有生成的代碼都正確引用共用類型

### 共用類型處理
1. 類型提取規則：
   - 識別在多個模塊中重複使用的類型
   - 將這些類型移動到 `shared-types.ts`
   - 在生成的代碼中使用導入引用

2. 實現方式：
   ```typescript
   // 1. 首次生成時收集所有類型
   const allTypes = collectAllTypes(generatedCode);
   
   // 2. 分析並提取共用類型
   const sharedTypes = extractSharedTypes(allTypes);
   
   // 3. 生成共用類型文件
   generateSharedTypesFile(sharedTypes);
   
   // 4. 生成最終代碼時使用共用類型
   generateFinalCode(sharedTypes);
   ```

### 文件結構
```
generated/
├── shared-types.ts     # 共用類型定義
├── baseApi.ts         # 基礎 API 配置
├── modules/           # 按模塊分割的 API 文件
│   ├── auth.ts
│   ├── audit.ts
│   └── operatorId.ts
└── types/            # 模塊特定的類型定義
    ├── auth.types.ts
    ├── audit.types.ts
    └── operatorId.types.ts
```

### 注意事項
1. 共用類型提取是一個兩階段過程，需要先執行一次完整的生成
2. 確保共用類型文件在最終生成之前已經準備好
3. 所有生成的代碼都應該正確引用共用類型
4. 避免在共用類型中引入循環依賴 
